[project]
name = "magicsquare"
version = "0.1.0"
description = "Python 매직 스퀘어 도메인 (ECB, TDD)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
